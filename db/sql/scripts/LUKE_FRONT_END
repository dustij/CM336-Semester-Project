USE MESOCYCLE_PLANNER;

SELECT *
FROM users
WHERE user_id = ?;

SELECT *
FROM user u, mesocycle_template m
WHERE u.user_id = m.created_by_user_id
AND m.template_id = ?;

SELECT * 
FROM planned_exercise pl, performed exercise pf
WHERE pf.status = 'SKIPPED';

